import { Component } from '@angular/core';

/** Solo se usa como placeholder para la ruta ''. El initialAuthGuard redirige antes de mostrar nada. */
@Component({
  selector: 'app-redirect',
  standalone: true,
  template: '',
})
export class RedirectComponent {}
